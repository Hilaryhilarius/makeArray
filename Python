import numpy as np

def makeArray(length):
    out = np.random.randint(0, 100, length)
    return out
#makeArray()

if __name__ == "__main__":
    print("Python child language")
    print(makeArray(10))
#main
